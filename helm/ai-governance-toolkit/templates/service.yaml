apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-aigovkit
  labels:
    app: {{ .Release.Name }}-aigovkit
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-aigovkit
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
